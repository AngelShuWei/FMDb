extends layout.pug

block content
    div(class='reviews')
        each reviewAndMovie in reviewsAndMovies
            div(class='registration-form')
                a(href=`http://localhost:8080/movies/${reviewAndMovie[1].id}`).movie
                    div Movie: #{reviewAndMovie[1].name}
                div Your Review: #{reviewAndMovie[0].content}
