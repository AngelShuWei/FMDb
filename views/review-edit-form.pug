extends layout.pug

include utils.pug

block content
    div(class="review-edit-container")
        form(action=`/reviews/${reviewId}/edit` method='POST' class="review-edit-form")
            +validationErrorSummary(errors)
            input(type="hidden" name="_csrf" value=csrfToken)
            input(type="hidden" name="movieId" value=pk)
            input(type="hidden" name="userId" value=userId)
            textarea(id="reviews-text-area", labelText="Review", name="content", class='edit-review-text')=reviewContent
            div(class="rating-edit-container")
                input(labelText="Rating", name="rating", type="number", min= '1', max='5', value=reviewRating, class="rating-input")
                p /5 ★

            div(class="registerCancelContainer")
            button(type="submit" class="register" id="registerLog") Add Review
