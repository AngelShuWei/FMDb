extends layout.pug

block content
  div(class="movie-list")
    each movie in movies
      div(class="movie" id=movie.id)
        a(href=`/movies/${movie.id}`)
          img(src=movie.imageURL, alt=movie.name, height="430px", width="300px" id="movie-poster" onclick="/movies/2")
        a(href=`/movies/${movie.id}`)
          p(class="movie-name")=movie.name
